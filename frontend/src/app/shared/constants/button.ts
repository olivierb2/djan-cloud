export enum ButtonStateEnum {
  'default' = 'default',
  'secondary' = 'secondary',
  'error' = 'error',
}

export enum ButtonTypeEnum {
  'button' = 'button',
  'submit' = 'submit',
  'reset' = 'reset',
}

export enum ButtonSizeEnum {
  'large' = 'large',
  'medium' = 'medium',
  'small' = 'small',
}

export enum ButtonStyleEnum {
  'primary' = 'primary',
  'stroke' = 'stroke',
  'text' = 'text',
  'filled-stroke' = 'filled-stroke',
}
